node_modules/
dist/
*.local
# Golden fixtures extracted from the real (client) workbook stay local-only.
tests/golden/fixtures/local/
# Build outputs of cowork/build-skill-zip.ps1: the copied estimator.html companion
# (../estimator.html is the source) and the shareable skill zip. Never commit either.
cowork/skills/d365-cost-estimate/estimator.html
cowork/dist/
.DS_Store
# Local Claude Code tooling (launch config, etc.) — personal, not part of the app.
.claude/
